#ifndef TYPEDEFS_H
#define TYPEDEFS_H

#include <QPair>
#include <QMap>
#include <QPoint>

typedef QPair<int, int> VertexIndex;
typedef QMap<VertexIndex, QPoint> Texels;

#endif // TYPEDEFS_H
